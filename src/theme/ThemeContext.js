import { createContext } from "react";

/**
 * This context is used for control application theme.
 *
 * Light themed components className has been changed to "lt" and Dark themed changed to "drk".
 * Testimonials table has been left alone.
 */

export const ThemeContext = createContext();